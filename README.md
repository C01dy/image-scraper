# 📸 Image Scraper - Автоматическое сохранение изображений

Простая утилита на Bun для автоматического скачивания и сохранения изображений с веб-камер или других источников.

## 🚀 Быстрый старт

### Локальный запуск

```bash
# Установка зависимостей (не требуется, но рекомендуется)
bun install

# Запуск с параметрами по умолчанию
bun start

# Запуск с кастомным URL
bun start --url http://example.com/camera.jpg

# Запуск с кастомной папкой
bun start --output ./my-images

# Полный пример
bun start --url http://example.com/cam.jpg --output /tmp/downloads
```

### Docker запуск

```bash
# Сборка образа
docker build -t image-scraper .

# Запуск с параметрами по умолчанию
docker-compose up

# Запуск с кастомными параметрами
docker run -v ./images:/app/images image-scraper bun app/index.js --url http://example.com/cam.jpg
```

## 📖 Использование

### Параметры командной строки

```bash
bun app/index.js [опции]
```

#### Опции:

| Флаг | Короткий | Описание | По умолчанию |
|------|----------|----------|--------------|
| `--url` | `-u` | URL изображения для загрузки | `http://apollo.sai.msu.ru/webcam.jpg` |
| `--output` | `-o` | Папка для сохранения изображений | `/app/images` (в Docker) или `./images` |
| `--help` | `-h` | Показать справку | - |

### Примеры

```bash
# Скачать изображение с веб-камеры МГУ
bun start

# Скачать с другого источника
bun start --url https://example.com/webcam/latest.jpg

# Сохранить в конкретную папку
bun start --output ./downloads

# Короткие флаги
bun start -u http://example.com/image.jpg -o ./pics

# Показать справку
bun start --help
```

### Переменные окружения

Вместо флагов можно использовать переменные окружения:

```bash
# Через переменные окружения
IMAGE_URL=http://example.com/cam.jpg OUTPUT_DIR=./photos bun start

# Или экспортировать
export IMAGE_URL=http://example.com/cam.jpg
export OUTPUT_DIR=./photos
bun start
```

## 🐳 Docker

### Сборка

```bash
docker build -t image-scraper .
```

### Запуск

```bash
# Через docker-compose (рекомендуется)
docker-compose up

# Напрямую с docker
docker run -v $(pwd)/images:/app/images image-scraper

# С кастомными параметрами
docker run -v $(pwd)/images:/app/images \
  -e IMAGE_URL=http://example.com/cam.jpg \
  image-scraper
```

### docker-compose.yml

```yaml
services:
  image-downloader:
    build: .
    container_name: image-downloader-app
    environment:
      - IMAGE_URL=http://apollo.sai.msu.ru/webcam.jpg
      - OUTPUT_DIR=/app/images
    volumes:
      - ./webcam_archive:/app/images
    # Для планировщика добавьте cron или используйте внешний планировщик
```

## ⏰ Автоматический запуск (Cron)

### Linux/macOS

```bash
# Редактировать crontab
crontab -e

# Запуск каждый час
0 * * * * cd /path/to/photo-saver && bun start --url http://example.com/cam.jpg --output /path/to/images

# Запуск каждые 30 минут
*/30 * * * * cd /path/to/photo-saver && bun start

# Запуск каждые 2 часа
0 */2 * * * cd /path/to/photo-saver && bun start
```

### Docker + Cron

Создайте `docker-compose.yml` с планировщиком:

```yaml
services:
  image-downloader:
    image: mcuadros/ofelia:latest
    depends_on:
      - scraper
    command: daemon --docker
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    labels:
      ofelia.job-run.scraper.schedule: "@every 1h"
      ofelia.job-run.scraper.container: "scraper"

  scraper:
    build: .
    container_name: scraper
    volumes:
      - ./images:/app/images
```

## 📁 Структура проекта

```
photo-saver/
├── app/
│   └── index.js          # Основной скрипт
├── images/               # Папка для сохранения (создается автоматически)
├── Dockerfile            # Docker образ
├── docker-compose.yml    # Docker Compose конфигурация
├── package.json          # Настройки проекта
└── README.md             # Документация
```

## 🎯 Формат имен файлов

Изображения сохраняются с именами вида:
```
image-2025-12-18_15-30-45.jpg
```

Формат: `image-YYYY-MM-DD_HH-MM-SS.<расширение>`

Расширение определяется автоматически из URL.

## 🔧 Требования

- **Bun** >= 1.0.0 (для локального запуска)
- **Docker** и **Docker Compose** (для контейнеризации)
- **Node.js** >= 20.0.0 (альтернатива Bun)

## 📝 Лицензия

MIT

## 🤝 Вклад

Форкайте, создавайте PR, открывайте issues!

## ⚡ Особенности

- ✅ Нет зависимостей (используются только встроенные модули)
- ✅ Быстрая работа благодаря Bun
- ✅ Поддержка различных форматов изображений (JPG, PNG, GIF, WebP)
- ✅ Автоматическое создание папок
- ✅ Детальное логирование
- ✅ Docker-ready
- ✅ Гибкая конфигурация через флаги или переменные окружения
